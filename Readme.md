# MERN Project

## ENV for backend

```

NODE_ENV=development
PORT=5000
MONGO_URI=mongo_url
JWT_SECRET=secrettoken
CLOUD_NAME=Get it from cloudinary
CLOUD_API_KEY=Get it from cloudinary
CLOUD_SECRET=Get it from cloudinary

```

## ENV for frontend

```
VITE_BACKEND_URL=http://localhost:5000/api
VITE_CLOUD_PRESET=Get it from cloudinary
VITE_CLOUD_NAME=Get it from cloudinary

```

## Requirements

- pnpm
- Node.js
- Express